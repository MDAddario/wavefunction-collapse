public class Driver {

    public static void main(String[] args) {

        /*
        // Create a board
        char[][] board = {{'L', 'L', 'L', 'L', 'L', 'L'},
                          {'L', 'L', 'L', 'C', 'C', 'L'},
                          {'L', 'L', 'C', 'S', 'S', 'C'},
                          {'L', 'C', 'S', 'S', 'S', 'S'},
                          {'C', 'S', 'S', 'S', 'S', 'S'}};

        // Set the correlation radius
        double radius = 1.0;

        // Create the sample object
        Sample sample = new Sample(board, radius);

        // Visualize the sample
        sample.visualize();

        // Create the wavefunction
        int height = 10;
        int width  = 10;
        Wavefunction phi = new Wavefunction(sample, height, width);

        // Run the simulations
        phi.quantumLoop();

        // Visualize collapsed version
        phi.visualizePhi();
         */
    }
}
